---
name: realship-fulfillment
description: 사방넷 → 풀필먼트 실배송 반자동화 스킬 (v0.5.6 — 전 매트릭스 lookup table화). 사용자가 사방넷에서 주문수집/일괄확정/엑셀 다운로드까지 직접 → 채팅에 엑셀 업로드 → Claude가 빈박스/실배송 분류 + 매핑 + 자가검증 보고서 → 사용자 OK 후 풀필먼트 업로드 엑셀(이더+뉴트리) 생성. 모든 룰 + 코드 lookup이 mapping_rules.json에 영구 박혀있어 재고조회 의존 없음. fixture_tests.json으로 매 사이클 회귀 테스트. 트리거: 실배송, 풀필먼트 등록, 발주등록, 풀필먼트 엑셀, 사방넷 발주, 스마트스토어 발주.
---

# 실배송 풀필먼트 (v0.5.19 — 가르시니아 + 11번가 -M개 일반화)

## v0.5.6 — 매트릭스 룰 전부 lookup table화 (2026-05-15)

재고조회 엑셀 의존을 **완전히 제거**. 사용자가 매 사이클 재고조회 다운로드 안 해도 됨.
- 구름깔창(루미솔/이더), 벌집깔창, 양말, 글램루아, 아쿠아슈즈 — 모든 matrix 룰의 코드를 `mapping_rules.json`의 lookup에 영구 박음 (총 85종)
- `resolve_matrix()` 일반화: 카테고리별 키 형식으로 lookup 우선 조회. mat 폴백은 신규 SKU 추가 대비용
- cross-validate에 **코드 변동 감지** 추가: 같은 주문번호인데 이전 사이클의 풀필먼트 코드와 다른 코드로 매핑되면 보고서에 알림 (정보용, stop X)
- **신규 SKU 추가 시**: mapping_rules.json의 해당 룰 lookup에 한 줄 추가

## v0.5.5 — 출고희망일 빈칸 + 우편번호 정규화 (2026-05-15)

- **출고희망일은 항상 빈칸**
- **우편번호 정규화 (`norm_zip`)**: 하이픈/공백 제거 + 5자리 zfill → leading 0 보존
- **우편번호 셀 text format** (`@`)

## 시스템 정보

### 사방넷 관리자 (사용자 직접)
URL: `https://sbadmin03.sabangnet.co.kr` / 로그인 `eithercompany / dlejzja7801!`

### 풀필먼트 (사용자 직접 업로드)
| 화주 | 회사코드 | 아이디 | 비번 |
|---|---|---|---|
| 이더컴퍼니 | w7298 | eithercompany | dlejrhddyd1@ |
| 뉴트리정 | w7298 | nutrijung | dlejrhddyd1@ |

URL: `https://wms02.sbfulfillment.co.kr`

## 워크플로우 (v0.5.6 간소화)

1. 사방넷 주문수집/일괄확정/엑셀 다운로드
2. 주문서 엑셀 1개만 채팅 업로드 (재고조회·발주조회 불필요)
3. Claude가 매핑 + cross-validate + 풀필먼트 엑셀 생성
4. 사용자 풀필먼트 사이트 업로드

```bash
python3 scripts/create_fulfillment_excel.py \
  --orders <주문서엑셀> \
  --prev-fulfillment-dir <Downloads또는워크스페이스폴더> \
  --state <Downloads>/realship_state.json --cycle-start "YYYY-MM-DD HH:MM" \
  --output-ether <이더엑셀> --output-nutri <뉴트리엑셀>
```

## 핵심 가드

1. shmaOrdNo 단일 키 매칭
2. 수량 룰 SKU N+N 셋트 단위
3. 빈박스 정확매칭
4. cross-validate 송장 중복 + 코드 변동 (v0.5.6)
5. 사이클 시작일 이전 SKIP
6. 헤더 23 컬럼 + 출고희망일 빈칸 (v0.5.5)
6b. 우편번호 정규화 (norm_zip + text format)
7. 이더(E*) vs 뉴트리(N*) 분리
8. 사용자 fallback (010-0000-0000 / 00000)
9. --report-only 모드 confirm 게이트
10. auto_stop_thresholds 자동 stop
11. fixture 회귀 테스트
12. 룰 충돌 감지
13. 합배송 정렬 (받는분+전화)
14. 코드 변동 감지 (v0.5.6)

## 변경 이력

- **v0.5.19** (2026-08-24) — **가르시니아 신규 매핑 + 11번가 수량룰 일반화**. ① 매핑룰 `뉴트리_가르시니아`(priority 63): 쿠팡 '체지방 감소 가르시니아 HCA 300정' → N00200028 (재고조회 확인). ② 신규 SKU 이름 5종 등록: N00200025 베르베린MAX / N00200027 올리브오일 / N00200028 가르시니아 / N00200029 콜라겐펩타이드 / N00200030 차전자피 (매핑룰은 샘플 도래 시 추가). ③ `event_n_count_11st` 일반화: '할인 이벤트:N개-M개' → ×N (M은 수량컬럼에 이미 반영. 멜라토닌 '1개-2개' 결제 51800=25900×2로 확정. 기존 -1개 한정 → -M개). ④ qty 가드에서 11번가 '-M개' 꼬리를 묶음수 탐지에서 제외 (M>1 구조적 오탐 제거). fixture 46 mapping + 4 address + 7 binbox.

- **v0.5.18** (2026-08-05) — **룰 2건 추가 + 빈박스 오분류 fix**. ① `toss_count_after_comma` 수량룰: 토스쇼핑 묶음 `{스펙}, N개`→×N. 멀티비타민 '300정, 2개' 결제금액 대조(43510 vs 단품 26900)로 2병 확정. 콤마 필수라 쿠팡 'N개 NNN정'·할인이벤트 미매칭. ② 매핑룰 `뉴트리_시크릿이너케어_질유산균_구명쿠팡`(priority 64): 쿠팡 '편안해질 질 유래 특허 유산균 180정' → N00200024. 원룰(63)과 동일우선순위 충돌 방지 위해 64. ③ `classify_binbox` `contains_exact` 정확일치 수정: substring 판정 탓에 '부재시 문 앞에 놓아주세요!'가 체험단으로 오분류→실배송 누락(7/31 전상철). strip 후 == 비교로 변경. fixture 43 mapping + 4 address + 7 binbox.

- **v0.5.17** (2026-07-06) — **룰 4건 추가 (2026-07-02~06 사이클 발견분)**. ① exclude_rules 디아핏 스니커즈/슬리퍼 실배송 제외 (all 2건 분리 — any 합치면 타 브랜드 슬리퍼 오폭). ② quantity_rules `n_count_bare` 옵션이 딱 'N개'만 (알파CD '2개' qty가드 검출). ③ mapping_rules 뉴트리_시크릿이너케어_질유산균 all:[이너케어]→N00200024 + sku_names (신규 SKU, 재고조회 확인). ④ quantity_rules `event_n_count_11st` 11번가 '할인 이벤트:N개-1개 (+가격)'→×N (콜린 결제금액 대조 확인, -1개 외 변형은 의도적 미매치→가드 stop). fixture 40종 (+4: 알파CD bare2개, 이너케어 신명/구명, 콜린 11번가).

- **v0.5.16** (2026-06-10) — **뉴트리 신규 3종 매핑 추가**. 코큐텐(N00200023)/MSM관절연골(N00200019)/NMN브로콜리(N00200020). 재고조회 2026-06-01 확인. fixture 3종 추가.
- **v0.5.15** (2026-06-09) — **'N박스' 수량누락 fix + 가드 광범위화**. 콜린 미오이노시톨 '300g 2박스'가 수량 1로 출고된 사고. (1) n_count_box 룰 'N박스'→×N 추가 (g 단위는 박스 앞이라 미매칭). (2) 수량 가드를 개→개/박스/팩/병+N+N으로 확장 — 새 단위가 나와도 조용히 1로 안 빠지고 auto-stop. (3) fixture 콜린 1/2박스 추가.
- **v0.5.14** (2026-06-05) — **쏘팔메토 수량누락 사고 fix**. (1) n_count_capsule 룰 'N개 NNN정/캡슐' 통합 — 'N개 180정' 옵션이 수량 1로 떨어지던 버그. (2) 수량룰 미적용 가드: 옵션 N개(N>=2)인데 수량 배수 미달 시 auto-stop (옷걸이 등 팩단위 룰은 qty_guard_exempt로 면제). (3) fixture 쏘팔메토 2개/3개·대마종자유 2개 추가.
- **v0.5.13** (2026-06-02) — **뉴트리 대마종자유 신규 SKU 추가**. mapping_rule 뉴트리_대마종자유_햄프씨드→N00200018, quantity_rule n_count_capsule(`N개 NNN캡슐`→×N), sku_name 등록. 재고조회 N00200018 확인.
- **v0.5.12** (2026-05-27) — 가드웰 무릎보호대도 실배송 제외(exclude_rules에 가드웰+무릎 추가). 발목보호대와 동일 정책.
- **v0.5.11** (2026-05-22) — **슬루나 실배송 제외 + 출고희망일 완전 비우기**. exclude_rules에 `none` 제외조건 지원(슬루나 룰에 `none:[베개]` → 호텔베개는 실배송 유지). 평가순서 개선: 송장SKIP→사이클이전→**빈박스→실배송제외**→실배송 (% 마스킹 슬루나/가드웰은 빈박스, % 없는 건 실배송제외). 출고희망일 빈 문자열('')도 값으로 취급되는 문제 → None + 셀 클리어로 완전 빈 셀.
- **v0.5.10** (2026-05-22) — **실배송 제외 룰 (exclude_rules)**. mapping_rules.json의 exclude_rules에 키워드 매칭(all/any) 시 해당 주문을 실배송 대상에서 완전 제외 (송장 SKIP 다음, 빈박스/매핑보다 먼저 평가). unmapped로 안 잡혀서 자동 stop 안 됨. 첫 적용: 가드웰 발목보호대(실배송 미발송, 담당자 별도 취소). 새 제외 상품은 exclude_rules에 한 줄 추가.
- **v0.5.9** (2026-05-20) — **동일 고객 재주문 누락 fix**. cross-validate fB(수취인+전화+코드) 매칭을 dup 판정에서 제거 → 정보용 카운터로만 표시. 며칠 후 같은 상품 재주문하는 정상 케이스가 더 이상 누락 X. fA(주문번호+코드)는 유지하여 진짜 중복 차단. 다른 모든 룰(매핑·합배송·우편번호·출고희망일·송장SKIP·lookup) 100% 무변경.
- **v0.5.8** (2026-05-19) — **송장번호 자동 SKIP 영구 룰**. 주문서의 '송장번호' 컬럼에 값이 있으면 자동 제외 (이미 풀필먼트 등록된 발주). cycle-start 인자 optional + default 1년 전 (송장 SKIP가 주 필터). 사용자가 매번 양식 정리할 필요 X.
- **v0.5.7** (2026-05-18) — **sku_names 영구 박음** (108종, E*+N*). 재고조회 엑셀 없어도 풀필먼트 엑셀 판매상품명이 정상 표시. to_xlsx 폴백 우선순위: SKU master > mapping_rules.sku_names > 룰 id.
- **v0.5.6** (2026-05-15) — 전 매트릭스 룰 lookup 영구화 (85종). resolve_matrix 일반화. 코드 변동 감지.
- **v0.5.5** (2026-05-15) — 출고희망일 빈칸 + 우편번호 norm_zip + text format. 아쿠아슈즈 lookup 28종.
- **v0.5.4** (2026-05-15) — 아쿠아슈즈 lookup table화.
- **v0.5.3** (2026-05-13) — 어제 풀필먼트 엑셀 자동 cross-validate.
- **v0.5.0** (2026-05-08) — 합배송 정렬.
- **v0.4.0** (2026-05-07) — 데이터 드리븐 룰 엔진.
